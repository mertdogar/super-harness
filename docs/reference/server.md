# @super-harness/server

The server package exports `harness(engine)` for composition and `serve()` for
a standalone host. It writes durable structural state to super-line
collections and broadcasts token deltas in thread rooms.

Install it with `pnpm add @super-harness/server @super-harness/core`.
