# @super-harness/shared

The shared package defines the contract fragment, collection row schemas, tree
fold, and client tree helpers. It is the compatibility boundary between the
server and clients.

Install it with `pnpm add @super-harness/shared`.
