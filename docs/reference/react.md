# @super-harness/react

The React package provides the framework-free `HarnessClient` plus
`HarnessProvider`, `useHarness()`, and `useHarnessClient()`. It contains no UI
components.

Install it with `pnpm add @super-harness/react`.
