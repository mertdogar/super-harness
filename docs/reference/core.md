# @super-harness/core

`createHarness()` creates the transport-free session runtime. It owns agents,
delegation, follow-up work, suspensions, approvals, modes, and threads.

Install it with `pnpm add @super-harness/core @mastra/core`.
