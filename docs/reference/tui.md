# @super-harness/tui

The terminal client provides an interactive OpenTUI cockpit and a headless
stdin/stdout shell. It requires Bun because OpenTUI uses `bun:ffi`.

Run it from the workspace with `pnpm -F @super-harness/tui start`.
