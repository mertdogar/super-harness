{
  "name": "@super-harness/shared",
  "version": "0.0.0",
  "description": "Isomorphic wire layer for Super Harness: super-line contract, the harness tree types, and the client-side Store view (subscribeTree + diffTree). No Mastra, no server deps — safe in the browser, Bun, and Node.",
  "license": "MIT",
  "author": "Mert Dogar",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@super-line/core": "^0.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
